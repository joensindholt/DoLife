{
  "name": "api",
  "version": "1.0.0",
  "description": "api for the minklub.dk project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -fr dist && tsc -p ./",
    "start": "node dist/index.js",
    "dynamodb": "java -Djava.library.path=/Applications/DynamoDB/DynamoDBLocal_lib -jar /Applications/DynamoDB/DynamoDBLocal.jar -sharedDb"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/joensindholt/goteamup/api"
  },
  "author": "Joen Sindholt",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.14.2",
    "express": "^4.13.4",
    "kerberos": "0.0.18"
  }
}
